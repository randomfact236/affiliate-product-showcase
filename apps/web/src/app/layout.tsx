export const metadata = {
  title: 'Affiliate Website',
  description: 'Enterprise Affiliate Platform',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
