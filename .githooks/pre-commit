#!/bin/sh
# Plan verification disabled — `plan/` scripts/files were removed.
# Skipping generation checks to avoid blocking commits.
set -e

echo "Note: plan/ verification skipped (plan/ removed)."

exit 0
